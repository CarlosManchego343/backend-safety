package Repository;

import org.springframework.data.jpa.repository.JpaRepository;
import Model.EvaluacionRiesgo;

public interface EvaluacionRiesgoRepository extends JpaRepository<EvaluacionRiesgo, Long> {}
